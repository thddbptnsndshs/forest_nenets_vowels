kind: 'formant' # kind of plot: duration or formant
filter_query: "syllable_count == 'polysyllabic' and consultant == 'AOK' and position == 'initial' and palatalization != 'none'"
#save_fn: 'images/OKT_formants.png' # only set save_fn to save the plot locally while running the function in a notebook/script
